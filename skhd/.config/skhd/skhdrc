# launch applications
alt - return : /Applications/kitty.app/Contents/MacOS/kitty -d ~
# alt - return : open -n -a "Ghostty"
# alt - n : /Applications/kitty.app/Contents/MacOS/kitty -d ~ lf
# alt - m : /Applications/iTerm.app/Contents/MacOS/iTerm2 -d ~ termusic
alt - m : open -a iTerm.app ~/.local/bin/termusic
# alt - return : /Applications/Hyper.app/Contents/MacOS/Hyper
# alt - return : kitty &
# alt - w : open -n -a "Arc"
alt - w : open -n -a "Brave Browser"
alt - y : open .
alt - r : /Applications/dmenu-mac.app/Contents/MacOS/dmenu-mac
# alt - r : open -a "$(find /Applications -name '*app' -maxdepth 1 | sed 's|/Applications/||;s|\.app||' | fzf --preview=)"
alt - e : open Desktop
# alt - m : "${HOME}"/.local/bin/launch_termusic
alt - backspace:  "${HOME}"/.local/bin/powermenu
# screenshot
